<?xml version="1.0" encoding="utf-8"?>
<manifest package="cn.dbboy.commentlib"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--<meta-data-->
        <!--android:name="./base/DBGlideModule"-->
        <!--android:value="GlideModule"/>-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".scalegradual.ScaleAndGradualActivity"/>
        <activity android:name=".heart.HeartFlowActivity"/>
        <activity android:name=".headpile.HeadPileActivity"/>
        <activity android:name=".toutiao.ToutiaoActivity"/>
        <activity android:name=".weibo.WeiboActivity"/>
    </application>

</manifest>